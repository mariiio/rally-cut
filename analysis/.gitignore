# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# Experiment results (regeneratable)
*.json
!rallycut.yaml
!**/manifest.json
!**/ground_truth.json
!baselines.json
!pyproject.toml
# Session 5 — small result/corpus files, committed for reproducibility.
!reports/occlusion_resolver/events.json
!reports/occlusion_resolver/event_scores.json

# Pseudo-labels and training frames (generated from database)
experiments/pseudo_labels*/
experiments/wasb_pseudo_labels/

# Training data and datasets (generated, large)
training_data/*
datasets/
# Session 5 — human-authored labels (small, not regeneratable).
!training_data/occlusion_resolver/
!training_data/occlusion_resolver/labels.json

# Training runs (ultralytics output)
runs/

# Debug output (generated images)
debug_*/

# Diagnostic outputs (regeneratable audit logs, plots, artifacts)
outputs/
reports/tracking_audit/
reports/within_team_reid/eval_cache/
reports/within_team_reid/thumbs/
reports/within_team_reid/sweep/
reports/within_team_reid/contact_sheet.html
reports/occlusion_resolver/crops/
reports/merge_veto/sweep/
reports/minimal_processing/sweep/

# Training artifacts (large, regeneratable)
reid_training_data/
reid_loocv_sweep.txt
experiments/wasb_gt_labels/

# Model weights (downloaded/trained)
weights/

# Vendor repos (cloned for training)
vendor/

# E2E-Spot experiment data (large, regeneratable)
data/spotting_frames/
data/spotting_frames.tar
data/spotting_metadata.json
data/external/

# Cache
.cache/
