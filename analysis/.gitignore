# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# Experiment results (regeneratable)
*.json
!rallycut.yaml
!**/manifest.json
!**/ground_truth.json
!baselines.json
!pyproject.toml

# Pseudo-labels and training frames (generated from database)
experiments/pseudo_labels*/
experiments/wasb_pseudo_labels/

# Training data and datasets (generated, large)
training_data/
datasets/

# Training runs (ultralytics output)
runs/

# Debug output (generated images)
debug_*/

# Diagnostic outputs (regeneratable audit logs, plots, artifacts)
outputs/

# Model weights (downloaded/trained)
weights/

# Vendor repos (cloned for training)
vendor/

# E2E-Spot experiment data (large, regeneratable)
data/spotting_frames/
data/spotting_frames.tar
data/spotting_metadata.json
data/external/

# Cache
.cache/
