# RallyCut Local Development Environment
# Uses MinIO, local PostgreSQL, and local FFmpeg
# Copy to .env for local development: cp .env.local.example .env

NODE_ENV=development
PORT=3001

# Database (local PostgreSQL via Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5436/rallycut?schema=public

# MinIO (S3-compatible local storage)
S3_ENDPOINT=http://localhost:9000
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
S3_BUCKET_NAME=rallycut-dev

# CloudFront (empty for local - videos served via API proxy)
CLOUDFRONT_DOMAIN=
CLOUDFRONT_KEY_PAIR_ID=
CLOUDFRONT_PRIVATE_KEY=

# Modal (ML detection - still uses Modal in local dev)
# Get these from your Modal deployment
MODAL_WEBHOOK_SECRET=local-dev-secret
MODAL_FUNCTION_URL=https://your-workspace--rallycut-detection-detect.modal.run

# CORS
CORS_ORIGIN=http://localhost:3000

# Lambda (empty for local - uses local FFmpeg)
EXPORT_LAMBDA_FUNCTION_NAME=
PROCESSING_LAMBDA_FUNCTION_NAME=
API_BASE_URL=http://localhost:3001
