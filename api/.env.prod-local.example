# RallyCut Production-Local Environment
# Uses real AWS S3, Lambda, and production database
# Run API locally but connect to production services
# Copy to .env: cp .env.prod-local.example .env
#
# WARNING: This connects to production services!

NODE_ENV=development
PORT=3001

# Database (production PostgreSQL)
DATABASE_URL=postgresql://user:password@prod-host:5432/rallycut?schema=public

# AWS S3 (production bucket - no S3_ENDPOINT means use real AWS)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
S3_BUCKET_NAME=rallycut-prod

# CloudFront (production CDN)
CLOUDFRONT_DOMAIN=dxxxxx.cloudfront.net
CLOUDFRONT_KEY_PAIR_ID=KXXXXX
CLOUDFRONT_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n..."

# Modal (ML detection)
MODAL_WEBHOOK_SECRET=your-prod-secret
MODAL_FUNCTION_URL=https://your-workspace--rallycut-detection-detect.modal.run

# CORS
CORS_ORIGIN=http://localhost:3000

# Lambda (production functions)
EXPORT_LAMBDA_FUNCTION_NAME=rallycut-video-export
PROCESSING_LAMBDA_FUNCTION_NAME=rallycut-video-optimize
API_BASE_URL=http://localhost:3001
